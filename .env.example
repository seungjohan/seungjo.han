# Admin Authentication
# These variables are used to secure the admin panel.
# For local development, you can set them here or leave them blank to use defaults.

# The key used to store the auth state in localStorage
VITE_ADMIN_AUTH_KEY=nahojgnuesAuth

# The value that must be stored in localStorage for the key
VITE_ADMIN_AUTH_VALUE=true
